name: NeuroEvolutionAgents
hp_initial:
  strength_mutation: 0.02

# ======================== Defaults sub-configs and other Hydra config ========================
defaults:
  - _self_
  - metrics: basic
